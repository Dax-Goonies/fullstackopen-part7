name: Bloglist tests

on:
  push:
    branches: [main]

jobs:
  tests:
    runs-on: ubuntu-latest
    services:
      mongodb:
        image: mongo:7
        ports:
          - 27017:27017
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install server dependencies
        working-directory: bloglist/server
        run: npm ci

      - name: Manually test server startup
        working-directory: bloglist/server
        run: |
          npm run start:test &
          sleep 5
          curl -v http://localhost:3003/api/blogs || echo "SERVER DID NOT RESPOND"
          kill %1 || true
        env:
          TEST_MONGODB_URI: mongodb://localhost:27017/bloglist-test
          SECRET: ci-test-secret-value
          PORT: 3003

      - name: Install client dependencies
        working-directory: bloglist/client
        run: npm ci

      - name: Install test dependencies
        working-directory: bloglist-tests
        run: npm ci

      - name: Install Playwright browsers
        working-directory: bloglist-tests
        run: npx playwright install --with-deps chromium

      - name: Run Playwright tests
        working-directory: bloglist-tests
        run: npx playwright test
        env:
          TEST_MONGODB_URI: mongodb://localhost:27017/bloglist-test
          SECRET: ci-test-secret-value
          PORT: 3003

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playright-report
          path: bloglist-tests/playwright-report/
          retention-days: 14